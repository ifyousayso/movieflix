class Movie {
	constructor(id, title, plot, categoryId, imageUrl) {
		this.id = id;
		this.title = title;
		this.plot = plot;
		this.categoryId = categoryId;
		this.imageUrl = imageUrl;
	}
}

export default Movie;
